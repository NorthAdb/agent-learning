# 第 11 课过关：包管理与虚拟环境

用户确认 0011 已按过关标准完成：能区分标准库 vs 第三方包；掌握 `python -m pip` / `install -r` / `list` / `show` / `freeze`；能读 `requirements-agent-demo.txt`；理解本仓库 `playground/python/.venv` 结构与「课件 vs 练习场」分工；能口述激活 vs 全路径调用、声明（requirements）vs 实例（.venv 不进 git）、README 约定 Python 大版本；五个练习脚本可跑通；遇 Windows GBK 读 requirements 中文注释问题已理解（清单用 ASCII 注释）。

## Implications
- clone Agent 仓起手式（venv → install -r → 验证）可作已知
- 下一课改为阶段 0 收尾（class + 对照读 North s01），不是异步扫盲。见 LR-0015。
