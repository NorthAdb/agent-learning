# Alvar skills 改名，避开 Matt 的 /teach

`npx skills add vasanthsreeram/Alvarmethod -g --all` 把 Alvar 的 `teach` 装进各 harness 的同名目录，盖掉了 Matt Pocock 的 `/teach`（多会话概念学习，工作区用 `MISSION.md`）。Alvar 整包已改名为 `alvar-*`。Matt 的 `/teach` 已从 `.cc-switch/skills/teach` 拷回 `~/.agents/skills/teach`。

## Implications
- 学这套课表：`/alvar-teach`（不是 `/teach`）
- Matt 的 `/teach` 已恢复；两套可以并存
