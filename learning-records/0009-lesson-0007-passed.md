# 第 7 课过关：文件读写与异常

用户确认 0007 已按过关标准完成：路径三案、`exists`/`resolve`、`r/w/a`、`with`/`encoding`、异常标准范式与边界三策略，以及 `load_knowledge` 式目录扫描均可讲解并跑通。

## Implications
- 文件 I/O 与异常范式可作后续课的已知前提（JSON 落盘、配置加载、工具失败边界）
- 当前主线进入 0008（JSON：配置与 tool_call）；不必再回头重讲 `Path`/`try` 基础
