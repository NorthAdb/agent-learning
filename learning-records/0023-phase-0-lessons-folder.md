# 阶段 0 课件迁到 lessons/phase-0/

用户要求前 16 课也按阶段进独立文件夹。0001–0016 从 `lessons/` 根目录迁到 `lessons/phase-0/`；课号与 playground/python 练习路径不变。课程表 `file` 改为相对 `lessons/` 的 `phase-0/…`；导航已能跨 `phase-0` / `phase-1` 跳转。

## Implications
- 全目录仍是 `lessons/index.html`；分册入口 `lessons/phase-0/index.html`
- 旧书签 `lessons/00NN-*.html` 会失效，应从目录或分册进
- 下一课仍是 0017（`lessons/phase-1/`）
